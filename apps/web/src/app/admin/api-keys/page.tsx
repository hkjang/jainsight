'use client';

import { useEffect, useState, useCallback } from 'react';
import { darkTheme, darkStyles } from '../../../components/admin/AdminUtils';

const API_URL = '/api';

interface ApiKey { id: string; name: string; keyPrefix: string; ownerId: string; ownerName?: string; scopes: string[]; rateLimit: number; usageCount: number; status: 'active' | 'revoked' | 'expired'; expiresAt?: string; lastUsedAt?: string; createdAt: string; ipWhitelist?: string[]; }
interface NewKeyResponse { id: string; key: string; keyPrefix: string; }

const scopeLabels: Record<string, string> = { 'query:read': '쿼리 조회', 'query:execute': '쿼리 실행', 'data:read': '데이터 조회', 'schema:read': '스키마 조회', 'admin:read': '관리자 조회' };
const availableScopes = Object.keys(scopeLabels);
const statusColors: Record<string, string> = { active: '#10B981', revoked: '#EF4444', expired: '#6B7280' };
const statusLabels: Record<string, string> = { active: '활성', revoked: '취소됨', expired: '만료됨' };

export default function ApiKeysAdminPage() {
    const [apiKeys, setApiKeys] = useState<ApiKey[]>([]);
    const [stats, setStats] = useState({ totalKeys: 5, activeKeys: 3, totalCalls: 28340, expiredKeys: 2 });
    const [loading, setLoading] = useState(true);
    const [searchTerm, setSearchTerm] = useState('');
    const [statusFilter, setStatusFilter] = useState('all');
    const [showCreateModal, setShowCreateModal] = useState(false);
    const [newKeyName, setNewKeyName] = useState('');
    const [newKeyScopes, setNewKeyScopes] = useState<string[]>(['query:read']);
    const [newKeyRateLimit, setNewKeyRateLimit] = useState(100);
    const [newKeyExpiry, setNewKeyExpiry] = useState('30');
    const [newKeyIPs, setNewKeyIPs] = useState('');
    const [generatedKey, setGeneratedKey] = useState<NewKeyResponse | null>(null);
    const [selectedKey, setSelectedKey] = useState<ApiKey | null>(null);
    const [showDetailsModal, setShowDetailsModal] = useState(false);
    const [usageData, setUsageData] = useState<{ date: string; count: number }[]>([]);

    const fetchApiKeys = useCallback(async () => { try { const res = await fetch(`${API_URL}/api-keys`); if (res.ok) setApiKeys(await res.json()); else setApiKeys([{ id: '1', name: 'Production API', keyPrefix: 'jsk_live_', ownerId: 'u1', ownerName: 'Admin', scopes: ['query:execute', 'data:read'], rateLimit: 1000, usageCount: 15420, status: 'active', createdAt: new Date(Date.now() - 30*24*60*60*1000).toISOString() }, { id: '2', name: 'Analytics Dashboard', keyPrefix: 'jsk_live_', ownerId: 'u2', ownerName: 'Analyst', scopes: ['query:read', 'data:read', 'schema:read'], rateLimit: 500, usageCount: 8920, status: 'active', createdAt: new Date(Date.now() - 60*24*60*60*1000).toISOString() }, { id: '3', name: 'Old Integration', keyPrefix: 'jsk_live_', ownerId: 'u3', ownerName: 'Developer', scopes: ['query:read'], rateLimit: 50, usageCount: 450, status: 'revoked', createdAt: new Date(Date.now() - 180*24*60*60*1000).toISOString() }]); } catch (e) { console.error(e); } }, []);
    const fetchKeyUsage = useCallback(async (keyId: string) => { const now = new Date(); setUsageData(Array.from({ length: 7 }, (_, i) => ({ date: new Date(now.getTime() - (6-i)*24*60*60*1000).toLocaleDateString('ko-KR'), count: Math.floor(Math.random() * 500) + 100 }))); }, []);

    useEffect(() => { fetchApiKeys().finally(() => setLoading(false)); }, [fetchApiKeys]);

    const handleCreateKey = async () => { if (!newKeyName || newKeyScopes.length === 0) return; setGeneratedKey({ id: 'new-1', key: `jsk_live_${Math.random().toString(36).substring(2, 15)}${Math.random().toString(36).substring(2, 15)}`, keyPrefix: 'jsk_live_' }); };
    const handleRevokeKey = async (keyId: string) => { if (!confirm('이 API 키를 취소하시겠습니까?')) return; console.log('Revoke', keyId); };
    const handleOpenDetails = async (key: ApiKey) => { setSelectedKey(key); await fetchKeyUsage(key.id); setShowDetailsModal(true); };
    const resetCreateForm = () => { setNewKeyName(''); setNewKeyScopes(['query:read']); setNewKeyRateLimit(100); setNewKeyExpiry('30'); setNewKeyIPs(''); setGeneratedKey(null); setShowCreateModal(false); };
    const toggleScope = (scope: string) => { setNewKeyScopes(prev => prev.includes(scope) ? prev.filter(s => s !== scope) : [...prev, scope]); };

    const filteredKeys = apiKeys.filter(key => { const matchesSearch = key.name.toLowerCase().includes(searchTerm.toLowerCase()) || key.keyPrefix.includes(searchTerm); return (statusFilter === 'all' || key.status === statusFilter) && matchesSearch; });

    if (loading) return <div style={{ ...darkStyles.container, display: 'flex', justifyContent: 'center', alignItems: 'center', minHeight: '400px' }}><div style={{ color: darkTheme.textSecondary }}>로딩 중...</div></div>;

    return (
        <div style={darkStyles.container}>
            <div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: '24px' }}><div><h1 style={{ fontSize: '28px', fontWeight: 'bold', color: darkTheme.textPrimary }}>🔑 API 키 관리</h1><p style={{ color: darkTheme.textSecondary, marginTop: '4px' }}>API 접근 키 생성 및 관리</p></div><button style={darkStyles.button} onClick={() => setShowCreateModal(true)}>+ API 키 생성</button></div>

            <div style={{ display: 'grid', gridTemplateColumns: 'repeat(auto-fit, minmax(180px, 1fr))', gap: '16px', marginBottom: '24px' }}>{[{ label: '전체 키', value: stats.totalKeys, icon: '🔑', color: darkTheme.accentBlue }, { label: '활성 키', value: stats.activeKeys, icon: '✅', color: darkTheme.accentGreen }, { label: '총 호출', value: stats.totalCalls.toLocaleString(), icon: '📊', color: darkTheme.accentPurple }, { label: '만료/취소', value: stats.expiredKeys, icon: '⚠️', color: darkTheme.accentRed }].map(stat => (<div key={stat.label} style={{ ...darkStyles.card, padding: '20px' }}><div style={{ display: 'flex', alignItems: 'center', gap: '12px' }}><div style={{ width: '44px', height: '44px', borderRadius: '10px', background: `${stat.color}20`, display: 'flex', alignItems: 'center', justifyContent: 'center', fontSize: '20px' }}>{stat.icon}</div><div><div style={{ fontSize: '24px', fontWeight: 'bold', color: stat.color }}>{stat.value}</div><div style={{ fontSize: '12px', color: darkTheme.textSecondary }}>{stat.label}</div></div></div></div>))}</div>

            <div style={{ ...darkStyles.card, marginBottom: '24px' }}><div style={{ padding: '16px', display: 'flex', gap: '12px', alignItems: 'center', flexWrap: 'wrap' }}><input type="text" placeholder="🔍 API 키 검색..." style={{ ...darkStyles.input, minWidth: '200px' }} value={searchTerm} onChange={e => setSearchTerm(e.target.value)} /><select style={darkStyles.input} value={statusFilter} onChange={e => setStatusFilter(e.target.value)}><option value="all">모든 상태</option><option value="active">활성</option><option value="revoked">취소됨</option><option value="expired">만료됨</option></select></div></div>

            <div style={darkStyles.card}><table style={{ width: '100%', borderCollapse: 'collapse' }}><thead><tr style={{ background: darkTheme.bgSecondary }}><th style={{ padding: '12px 16px', textAlign: 'left', fontSize: '12px', fontWeight: '600', color: darkTheme.textMuted }}>API 키</th><th style={{ padding: '12px 16px', textAlign: 'left', fontSize: '12px', fontWeight: '600', color: darkTheme.textMuted }}>Scope</th><th style={{ padding: '12px 16px', textAlign: 'center', fontSize: '12px', fontWeight: '600', color: darkTheme.textMuted }}>Rate Limit</th><th style={{ padding: '12px 16px', textAlign: 'center', fontSize: '12px', fontWeight: '600', color: darkTheme.textMuted }}>사용량</th><th style={{ padding: '12px 16px', textAlign: 'center', fontSize: '12px', fontWeight: '600', color: darkTheme.textMuted }}>상태</th><th style={{ padding: '12px 16px', textAlign: 'right', fontSize: '12px', fontWeight: '600', color: darkTheme.textMuted }}>작업</th></tr></thead><tbody>{filteredKeys.map(key => (<tr key={key.id} style={{ borderBottom: `1px solid ${darkTheme.borderLight}` }}><td style={{ padding: '16px' }}><div style={{ fontWeight: '500', color: darkTheme.textPrimary }}>{key.name}</div><code style={{ fontSize: '11px', color: darkTheme.textMuted, background: darkTheme.bgInput, padding: '2px 4px', borderRadius: '4px' }}>{key.keyPrefix}***</code>{key.ownerName && <div style={{ fontSize: '11px', color: darkTheme.textMuted, marginTop: '2px' }}>by {key.ownerName}</div>}</td><td style={{ padding: '16px' }}><div style={{ display: 'flex', gap: '4px', flexWrap: 'wrap' }}>{key.scopes.slice(0, 2).map(scope => (<span key={scope} style={{ padding: '2px 6px', background: `${darkTheme.accentPurple}20`, color: darkTheme.accentPurple, fontSize: '10px', borderRadius: '4px' }}>{scopeLabels[scope] || scope}</span>))}{key.scopes.length > 2 && (<span style={{ padding: '2px 6px', background: darkTheme.bgSecondary, color: darkTheme.textMuted, fontSize: '10px', borderRadius: '4px' }}>+{key.scopes.length - 2}</span>)}</div></td><td style={{ padding: '16px', textAlign: 'center', fontSize: '14px', color: darkTheme.textSecondary }}>{key.rateLimit}/분</td><td style={{ padding: '16px', textAlign: 'center', fontWeight: '500', fontSize: '14px', color: darkTheme.textPrimary }}>{key.usageCount.toLocaleString()}</td><td style={{ padding: '16px', textAlign: 'center' }}><span style={{ padding: '4px 8px', background: `${statusColors[key.status]}20`, color: statusColors[key.status], fontSize: '11px', fontWeight: '500', borderRadius: '4px' }}>{statusLabels[key.status]}</span></td><td style={{ padding: '16px', textAlign: 'right' }}><button onClick={() => handleOpenDetails(key)} style={{ padding: '4px 12px', background: `${darkTheme.accentPurple}20`, color: darkTheme.accentPurple, border: 'none', borderRadius: '6px', fontSize: '12px', cursor: 'pointer', marginRight: '8px' }}>상세</button>{key.status === 'active' && (<button onClick={() => handleRevokeKey(key.id)} style={{ padding: '4px 12px', background: `${darkTheme.accentRed}20`, color: darkTheme.accentRed, border: 'none', borderRadius: '6px', fontSize: '12px', cursor: 'pointer' }}>취소</button>)}</td></tr>))}</tbody></table>{filteredKeys.length === 0 && (<div style={{ padding: '48px', textAlign: 'center', color: darkTheme.textMuted }}>API 키가 없습니다</div>)}</div>

            {showCreateModal && (<div style={darkStyles.modalOverlay} onClick={resetCreateForm}><div style={darkStyles.modal} onClick={e => e.stopPropagation()}>{!generatedKey ? (<><h2 style={{ fontSize: '20px', fontWeight: 'bold', marginBottom: '20px', color: darkTheme.textPrimary }}>API 키 생성</h2><div style={{ marginBottom: '16px' }}><label style={{ display: 'block', fontSize: '14px', fontWeight: '500', marginBottom: '6px', color: darkTheme.textSecondary }}>키 이름 *</label><input type="text" style={{ ...darkStyles.input, width: '100%' }} value={newKeyName} onChange={e => setNewKeyName(e.target.value)} placeholder="예: Production API" /></div><div style={{ marginBottom: '16px' }}><label style={{ display: 'block', fontSize: '14px', fontWeight: '500', marginBottom: '8px', color: darkTheme.textSecondary }}>Scope *</label><div style={{ display: 'flex', flexWrap: 'wrap', gap: '8px' }}>{availableScopes.map(scope => (<button key={scope} onClick={() => toggleScope(scope)} style={{ padding: '6px 12px', background: newKeyScopes.includes(scope) ? darkTheme.accentPurple : darkTheme.bgSecondary, color: newKeyScopes.includes(scope) ? 'white' : darkTheme.textSecondary, border: 'none', borderRadius: '6px', fontSize: '12px', cursor: 'pointer' }}>{scopeLabels[scope]}</button>))}</div></div><div style={{ marginBottom: '16px' }}><label style={{ display: 'block', fontSize: '14px', fontWeight: '500', marginBottom: '6px', color: darkTheme.textSecondary }}>Rate Limit (요청/분)</label><input type="number" style={{ ...darkStyles.input, width: '100%' }} value={newKeyRateLimit} onChange={e => setNewKeyRateLimit(Number(e.target.value))} min={1} /></div><div style={{ marginBottom: '16px' }}><label style={{ display: 'block', fontSize: '14px', fontWeight: '500', marginBottom: '6px', color: darkTheme.textSecondary }}>만료 기간</label><select style={{ ...darkStyles.input, width: '100%' }} value={newKeyExpiry} onChange={e => setNewKeyExpiry(e.target.value)}><option value="7">7일</option><option value="30">30일</option><option value="90">90일</option><option value="365">1년</option><option value="never">무제한</option></select></div><div style={{ marginBottom: '24px' }}><label style={{ display: 'block', fontSize: '14px', fontWeight: '500', marginBottom: '6px', color: darkTheme.textSecondary }}>IP 화이트리스트 (쉼표 구분)</label><input type="text" style={{ ...darkStyles.input, width: '100%' }} value={newKeyIPs} onChange={e => setNewKeyIPs(e.target.value)} placeholder="192.168.1.1, 10.0.0.0/24" /></div><div style={{ display: 'flex', gap: '12px', justifyContent: 'flex-end' }}><button style={darkStyles.buttonSecondary} onClick={resetCreateForm}>취소</button><button style={darkStyles.button} onClick={handleCreateKey}>생성</button></div></>) : (<><div style={{ textAlign: 'center', marginBottom: '24px' }}><div style={{ fontSize: '48px', marginBottom: '12px' }}>🔑</div><h2 style={{ fontSize: '20px', fontWeight: 'bold', color: darkTheme.accentGreen }}>API 키 생성 완료!</h2></div><div style={{ background: `${darkTheme.accentYellow}20`, border: `1px solid ${darkTheme.accentYellow}`, borderRadius: '8px', padding: '12px', marginBottom: '16px' }}><div style={{ display: 'flex', alignItems: 'center', gap: '8px', color: darkTheme.accentYellow, fontWeight: '500', fontSize: '14px' }}>⚠️ 이 키는 다시 표시되지 않습니다!</div></div><div style={{ background: darkTheme.bgInput, borderRadius: '8px', padding: '16px', marginBottom: '24px' }}><div style={{ fontSize: '12px', color: darkTheme.textMuted, marginBottom: '8px' }}>API Key</div><div style={{ fontFamily: 'monospace', fontSize: '14px', wordBreak: 'break-all', color: darkTheme.textPrimary, userSelect: 'all' }}>{generatedKey.key}</div></div><button style={{ ...darkStyles.button, width: '100%' }} onClick={() => { navigator.clipboard.writeText(generatedKey.key); alert('클립보드에 복사되었습니다!'); }}>📋 복사하기</button><button style={{ ...darkStyles.buttonSecondary, width: '100%', marginTop: '12px' }} onClick={resetCreateForm}>닫기</button></>)}</div></div>)}

            {showDetailsModal && selectedKey && (<div style={darkStyles.modalOverlay} onClick={() => setShowDetailsModal(false)}><div style={{ ...darkStyles.modal, maxWidth: '600px' }} onClick={e => e.stopPropagation()}><div style={{ display: 'flex', justifyContent: 'space-between', alignItems: 'center', marginBottom: '24px' }}><div><h2 style={{ fontSize: '20px', fontWeight: 'bold', color: darkTheme.textPrimary }}>{selectedKey.name}</h2><code style={{ fontSize: '12px', color: darkTheme.textMuted }}>{selectedKey.keyPrefix}***</code></div><span style={{ padding: '6px 12px', background: `${statusColors[selectedKey.status]}20`, color: statusColors[selectedKey.status], borderRadius: '6px', fontWeight: '500' }}>{statusLabels[selectedKey.status]}</span></div><div style={{ display: 'grid', gridTemplateColumns: '1fr 1fr', gap: '16px', marginBottom: '24px' }}><div style={{ background: darkTheme.bgSecondary, padding: '16px', borderRadius: '8px' }}><div style={{ fontSize: '12px', color: darkTheme.textMuted, marginBottom: '4px' }}>총 사용량</div><div style={{ fontSize: '24px', fontWeight: 'bold', color: darkTheme.textPrimary }}>{selectedKey.usageCount.toLocaleString()}</div></div><div style={{ background: darkTheme.bgSecondary, padding: '16px', borderRadius: '8px' }}><div style={{ fontSize: '12px', color: darkTheme.textMuted, marginBottom: '4px' }}>Rate Limit</div><div style={{ fontSize: '24px', fontWeight: 'bold', color: darkTheme.textPrimary }}>{selectedKey.rateLimit}/분</div></div></div><div style={{ marginBottom: '24px' }}><div style={{ fontSize: '14px', fontWeight: '500', marginBottom: '12px', color: darkTheme.textPrimary }}>7일 사용량 추이</div><div style={{ display: 'flex', alignItems: 'flex-end', gap: '8px', height: '120px', background: darkTheme.bgSecondary, borderRadius: '8px', padding: '16px' }}>{usageData.map((d, i) => (<div key={i} style={{ flex: 1, display: 'flex', flexDirection: 'column', alignItems: 'center' }}><div style={{ width: '100%', height: `${Math.max((d.count / 500) * 80, 10)}px`, background: `linear-gradient(180deg, ${darkTheme.accentBlue}, #60A5FA)`, borderRadius: '4px 4px 0 0' }} /><div style={{ fontSize: '10px', color: darkTheme.textMuted, marginTop: '4px' }}>{d.date.split('.').slice(1).join('/')}</div></div>))}</div></div><div style={{ marginBottom: '24px' }}><div style={{ fontSize: '14px', fontWeight: '500', marginBottom: '8px', color: darkTheme.textPrimary }}>Scopes</div><div style={{ display: 'flex', gap: '8px', flexWrap: 'wrap' }}>{selectedKey.scopes.map(scope => (<span key={scope} style={{ padding: '6px 12px', background: `${darkTheme.accentPurple}20`, color: darkTheme.accentPurple, fontSize: '12px', borderRadius: '6px' }}>{scopeLabels[scope] || scope}</span>))}</div></div><button style={{ ...darkStyles.buttonSecondary, width: '100%' }} onClick={() => setShowDetailsModal(false)}>닫기</button></div></div>)}
        </div>
    );
}
