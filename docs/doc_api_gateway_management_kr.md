# API 관리 (API Management)

SQL 기반 API의 수명 주기를 모니터링하고 제어합니다.

## API 카드

생성된 각 API는 다음 정보를 포함하는 카드로 표시됩니다:

- **메서드**: HTTP 메서드를 나타냅니다 (주로 GET/POST).
- **파라미터 수**: 허용되는 동적 입력의 개수입니다.
- **SQL 미리보기**: 기본 로직에 대한 요약입니다.

## 보안 및 액세스

- **API 키**: 각 API는 고유한 API 키로 보호됩니다.
  - **가시성**: "눈" (👁️) 아이콘을 클릭하여 키를 표시합니다.
  - **복사**: 안전한 공유를 위한 원클릭 복사를 지원합니다.
- **엔드포인트**: 전체 URL 엔드포인트가 자동으로 생성됩니다. "엔드포인트 복사" 버튼을 사용하여 프론트엔드나 클라이언트 애플리케이션에서 사용할 URL을 가져오세요.

## 문서화 (Documentation)

- **Docs 버튼**: 특정 엔드포인트에 대한 OpenAPI / Swagger 인터페이스를 자동 생성하여, 외부 팀이 API 사용 방법을 쉽게 이해할 수 있도록 돕습니다.
